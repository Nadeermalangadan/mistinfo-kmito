{% for photo in photos %}
	{% if category.description %}
		<a target="_blank" href="{{ category.description }}">
	{% endif %}
		<img src="{{ photo.getThumb('[120x450]') }}" title="{{ photo.description }}" alt="{{ photo.description }}" />
	{% if category.description %}
		</a>
	{% endif %}
{% endfor %}